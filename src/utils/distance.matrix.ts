import * as distance from 'google-distance-matrix';

export class DistanceMatrix {

    public static matrix(...origins: string[], ...destinations: string[]) {

    }

    public static point() {

    }
}